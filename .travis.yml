language: minimal
dist: xenial
install: wget -qO- --retry-connrefused https://storage.googleapis.com/matlabimagesus/public/install.sh | sudo -E bash
#script: matlab -batch "runAllMyTests"
script: matlab -batch 'matlabroot,ver'

